import org.specs2.mutable._
import play.api.test._
import play.api.test.Helpers._

class VersionSpec extends Specification {
  
  "A Version" should {
    "be insertable" in { pending }
  
    "on creation, also create a document in alfresco" in { pending }
    
    "on creation, populate the created field with the current date and time" in { pending }
    
    "on creation, populate the 'created by' field with the current user" in { pending }
    
    "be able to retrieve a document from alfresco" in { pending }
  }

}