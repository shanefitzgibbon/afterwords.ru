import org.scalatest.FlatSpec

class VersionSpec extends FlatSpec {
  
  "A Version" should "be insertable" is (pending)
  
  it should "on creation, also create a document in alfresco" is (pending)
  
  it should "on creation, populate the created field with the current date and time" is (pending)
  
  it should "on creation, populate the 'created by' field with the current user" is (pending)
  
  it should "be able to retrieve a document from alfresco" is (pending)

}