---
applications:
  ./dist/afterwords-jobs-0.1-SNAPSHOT.zip:
    name: afterwords
    framework:
      name: play
      info:
        mem: 256M
        description: Play Framework Application
        exec: 
    url: ${name}.${target-base}
    mem: 256M
    instances: 1
    services:
      mongodb-cedc9:
        type: mongodb
